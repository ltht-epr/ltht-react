schema: http://localhost:5000/graphql
config:
  skipTypename: true
  scalars:
    Date: string
    DateTime: string
    DateTimeOffset: string
    Decimal: number
    Milliseconds: number
    Seconds: number
generates:
  src/atoms/graphql-types.ts:
    - typescript
