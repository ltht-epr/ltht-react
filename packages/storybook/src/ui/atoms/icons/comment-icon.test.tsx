import { render } from '@testing-library/react'

import { CommentIcon } from '@ltht-react/icon'

describe('Comment Icon', () => {
  it('Renders', () => {
    render(<CommentIcon size="small" />)
  })
})
