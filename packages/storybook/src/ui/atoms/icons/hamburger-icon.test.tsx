import { render } from '@testing-library/react'

import { HamburgerIcon } from '@ltht-react/icon'

describe('Comment Icon', () => {
  it('Renders', () => {
    render(<HamburgerIcon size="small" />)
  })
})
