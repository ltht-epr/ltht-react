import { render } from '@testing-library/react'

import { ExternalLinkIcon } from '@ltht-react/icon'

describe('External Link Icon', () => {
  it('Renders', () => {
    render(<ExternalLinkIcon size="small" />)
  })
})
