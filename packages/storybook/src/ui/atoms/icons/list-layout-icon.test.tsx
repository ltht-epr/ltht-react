import { render } from '@testing-library/react'

import ListLayoutIcon from '@ltht-react/icon/src/atoms/list-layout-icon'

describe('List Layout Icon', () => {
  it('Renders', () => {
    render(<ListLayoutIcon size="small" />)
  })
})
