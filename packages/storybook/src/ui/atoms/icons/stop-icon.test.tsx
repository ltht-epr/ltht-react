import { StopIcon } from '@ltht-react/icon'
import { render } from '@testing-library/react'

describe('Stop Icon', () => {
  it('Renders', () => {
    render(<StopIcon size="small" />)
  })
})
