import { render } from '@testing-library/react'

import { FolderPlusIcon } from '@ltht-react/icon'

describe('Folder Plus Icon', () => {
  it('Renders', () => {
    render(<FolderPlusIcon size="small" />)
  })
})
