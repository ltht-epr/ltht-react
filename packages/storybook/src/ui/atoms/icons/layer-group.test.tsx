import { render } from '@testing-library/react'

import { LayerGroupIcon } from '@ltht-react/icon'

describe('Layer Group Icon', () => {
  it('Renders', () => {
    render(<LayerGroupIcon size="small" />)
  })
})
