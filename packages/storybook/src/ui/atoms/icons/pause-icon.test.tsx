import { PauseIcon } from '@ltht-react/icon'
import { render } from '@testing-library/react'

describe('Pause Icon', () => {
  it('Renders', () => {
    render(<PauseIcon size="small" />)
  })
})
