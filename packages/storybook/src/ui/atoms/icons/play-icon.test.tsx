import { PlayIcon } from '@ltht-react/icon'
import { render } from '@testing-library/react'

describe('Play Icon', () => {
  it('Renders', () => {
    render(<PlayIcon size="small" />)
  })
})
