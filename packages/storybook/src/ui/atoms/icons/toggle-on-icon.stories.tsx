import { render } from '@testing-library/react'

import ToggleOnIcon from '@ltht-react/icon/src/atoms/toggle-on-icon'

describe('Toggle On Icon', () => {
  it('Renders', () => {
    render(<ToggleOnIcon size="small" />)
  })
})
