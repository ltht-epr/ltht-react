import styled from '@emotion/styled'

const ListItem = styled.li`
  display: flex;
  margin: 0;
  padding: 0;
  text-indent: 0;
  padding: 0.8rem;

  &:hover {
    background: #15398d;
    cursor: pointer;
  }
`

export default ListItem
