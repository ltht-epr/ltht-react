import { FC } from 'react'
import styled from '@emotion/styled'

const StyledDueDate = styled.div``

const DueDate: FC = ({ children }) => <StyledDueDate>{children}</StyledDueDate>

export default DueDate
